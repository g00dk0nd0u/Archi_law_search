# AGENTS.md

## 目的

このリポジトリは、建築関連法令を `laws.db` から検索し、根拠条文付きで正確に回答するためのものです。

法令回答では、一般知識ではなく、必ず DB の検索結果を根拠にする。

---

## 絶対ルール

- 建築法令の質問では、ウェブ検索をしない。
- 一般知識だけで回答しない。
- 必ず `laws.db` を検索してから回答する。
- DBで確認できない内容は、無理に補完せず「このDB上では確認できません」と書く。
- 回答では、必ず以下を明示する。
  - 法令名
  - 条番号
  - 条名
  - 該当する原文

---

## 検索ルール

### 条番号が分かる場合

条番号を指定して検索する。

例：

```python
from pathlib import Path
from search_laws import search_laws

result = search_laws(
    db_path=Path("data/laws.db"),
    law="建築基準法",
    article="第五十二条",
    limit=10,
)
````

### 条番号が分からない場合

キーワードで検索する。

例：

```python
result = search_laws(
    db_path=Path("data/laws.db"),
    query="容積率 前面道路",
    limit=10,
)
```

### 再検索

初回検索で不十分な場合のみ、検索語を変えて最大1回だけ再検索する。

例：

* 初回：`容積率`
* 再検索：`前面道路 容積率`

---

## 回答形式

回答は原則として、次の3項目だけにする。

```md
## 結論

（DBで確認できた内容だけを短く書く）

## 根拠条文

- 法令名：
- 条番号：
- 条名：

## 原文

（該当する条文原文をそのまま引用する）
```

---

## 複数条文が関係する場合

条文ごとに分けて書く。

```md
## 結論

（複数条文を踏まえた結論を短く書く）

## 根拠条文

### 1. 建築基準法 第五十二条（容積率）

（この条文が何を定めているかを1行で書く）

### 2. 建築基準法施行令 第二条（面積、高さ等の算定方法）

（この条文が何を定めているかを1行で書く）

## 原文

### 建築基準法 第五十二条（容積率）

（該当原文）

### 建築基準法施行令 第二条（面積、高さ等の算定方法）

（該当原文）
```

---

## 書き方のルール

* 先に結論を書く。
* 原文と解釈を混ぜない。
* 原文は改変しない。
* 長い条文は、該当部分だけ抜粋する。
* 抜粋する場合でも、意味が変わらない範囲にする。
* 「代表的には」「主に」「など」はなるべく使わない。
* 網羅できない場合は「このDB上で確認できた範囲では」と書く。
* 実務上の補足は原則として書かない。
* DB外の推測を書く場合は、必ず「DB外の補足」と明示する。

---

## 悪い回答例

```md
容積率は延べ面積を敷地面積で割ります。
道路幅員制限もあります。
実務では不算入部分も見る必要があります。
必要なら具体計算します。
```

問題点：

* 根拠条文がない
* 原文がない
* DB確認範囲が不明
* 実務補足が混ざっている
* 最後の提案が不要

---

## 良い回答例

```md
## 結論

容積率は、建築物の延べ面積の敷地面積に対する割合です。

## 根拠条文

- 法令名：建築基準法
- 条番号：第五十二条
- 条名：（容積率）

## 原文

建築物の延べ面積の敷地面積に対する割合（以下「容積率」という。）は、次の各号に掲げる区分に従い、当該各号に定める数値以下でなければならない。
```

---

## 回答前チェック

回答前に必ず確認する。

* DBを検索したか
* 条文番号を確認したか
* 条名を確認したか
* 原文を出したか
* DB外の説明を混ぜていないか
* 回答が「結論／根拠条文／原文」の形になっているか
