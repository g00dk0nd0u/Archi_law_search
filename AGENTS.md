# AGENTS.md

- このリポジトリは、Webアプリ開発と Codex 法令調査の両方に使います。
- 法令調査では、必ず `python -m cli.search_laws` で `data/laws.db` を検索して確認します。
- macOS などで `python` が使えない場合は、`python3 -m cli.search_laws` に読み替えます。Windows、macOS、Codexクラウド環境で Python コマンド名が異なる場合があります。
- 一般知識だけで法令回答しません。
- 回答は原則として `## まとめ` / `## 根拠条文` / `## 原文` の Markdown 形式で整理します。
- 回答には、法令名・条番号・条名・該当原文を含めます。複数条文がある場合は条文ごとに分けます。
- DBで確認できない内容は、「このDB上では確認できません」と明記します。
- `--article` は前方一致検索であり、枝番条文も含みます。
- 先にまとめを書き、原文と解釈を混ぜません。
- 原文は省略しすぎません。最低でも該当する項単位で全文を出し、短い条文であれば条全体を全文で出します。
- 号が論点の場合でも、必要に応じてその号だけでなく、対応する項全体を確認できる形で出します。
- 原文は改変せず、`...` による省略は原則避けます。どうしても省略する場合は、省略箇所を明示し、意味が変わらない範囲に限定します。
- `## 根拠条文` では、各条文ごとに `### 1. 法令名 条番号（条名）` の形で見出しを立て、その条文が何を定めているかを1行で書きます。
- `## 原文` では、各条文ごとに `### 法令名 条番号（条名）` の形で見出しを立て、最低でも該当項単位で全文を出します。
- ユーザーから特別な指示があるまではウェブで調べず、ウェブ情報も使いません。
- 網羅できない場合は「このデータベース上で確認できた範囲では」と書き、DB外の推測は必ず `DB外の補足` と明示します。
- 実務上の補足は原則として書かず、「代表的には」「主に」「など」「おそらく」「たぶん」はなるべく使いません。
- ユーザー向けの最終回答には、検索過程やコマンド実行回数は原則として書きません。レビュー・デバッグ・動作確認として求められた場合や、実行環境差の説明が必要な場合だけ簡潔に書きます。
- 回答前に、DB検索・条文番号・条名・原文・非DB説明の混入有無・`まとめ/根拠条文/原文` の形を確認します。
- 開発時は既存Webアプリを壊さないことを優先します。
- 詳細は `docs/CODEX_USAGE.md` / `docs/DEVELOPMENT.md` / `docs/DB_POLICY.md` を参照してください。
