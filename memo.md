以下は、未だ実装しない単なる覚書です。

【タイトル】
リンク生成ルールに関する課題整理メモ

【目的】
将来的に app.py 内で、e-Govと同様に条文内のリンクを自動生成し、別ウィンドウで該当法令を表示できるようにすること。

【背景】
e-GovのHTMLには本文やリンク情報が直接含まれていない。リンクはJavaScriptによる動的生成であり、データとして持っているのではなく「規則ベース」で生成されている可能性が高い。

【重要な発見】
- リンクは「文字列パターン（第○条、第○項、第○号）」を検出して生成されている。
- URLは「条・項・号」を座標として組み立てられている。
- 例：
  https://laws.e-gov.go.jp/law/325CO0000000338#Mp-At_108_2-Pr_1-It_2
  → At_108_2 = 第108条の2
     Pr_1     = 第1項
     It_2     = 第2号

【推定ルール】
- 第◯条       → At_◯
- 第◯条のn    → At_◯_n
- 第◯項       → Pr_◯
- 第◯号       → It_◯
- URL形式は：
  <LAW_ID>#Mp-[At_x][_n]-[Pr_y]-[It_z]

【結論】
e-Govのリンクはデータを丸ごとコピーする必要はなく、ルールを再現すれば生成可能である。

【今後のタスク（後で実施）】
1. 本文中のリンク候補語句（第○条など）を正規表現で抽出する。
2. 抽出した語句を座標形式に変換する。
3. URLを組み立てる関数を作成する。
4. 別ウィンドウで該当条文を開く処理を実装する。

【現時点】
本ルールの実装は行わず、後日Codexに依頼する。
